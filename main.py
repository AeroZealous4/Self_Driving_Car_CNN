import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils import data
from torch.utils.data import DataLoader
import torchvision.transforms as transforms
import cv2
import numpy as np
import csv

import controller 
#Data Aquisition 

#Preprocessing

#Creating Model "controller.py" (Architecture, Corresponding Code etc)

#Training (Generalized for model in "controller.py")

#Generate h5 file "controller.h5" which have trained model weights

#Create seperate file "drive.py" which will use "controller.py" & 
